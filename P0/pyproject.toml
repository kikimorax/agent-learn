[project]
name = "p0"
version = "0.1.0"
description = "Phase 0: Python engineering and local model gateway probing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agent-common",
    "httpx>=0.28.1",
    "pydantic>=2.10",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
]


[tool.uv.sources]
agent-common = { path = "../shared", editable = true }
