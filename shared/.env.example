# Required: OpenAI-compatible local proxy and a model exposed by that proxy.
LLM_BASE_URL=http://127.0.0.1:3030/v1
LLM_MODEL=

# Optional: leave empty when the local proxy does not require authentication.
LLM_API_KEY=
